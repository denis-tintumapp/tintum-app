rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas por defecto - ajustar según necesidades del proyecto
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

